import 'package:flutter/material.dart';

class AppColors {
  static const success = Color(0xFF00AA00);
  static const error = Color(0xFFFF0000);
  static const unusedBackground = Color(0x2EFFFFFF);
  static const unusedText = Color(0x80FFFFFF);
}